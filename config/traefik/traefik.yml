api:
  dashboard: true
  insecure: true  # dashboard at http://localhost:8080 — dev only

entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

log:
  level: INFO
